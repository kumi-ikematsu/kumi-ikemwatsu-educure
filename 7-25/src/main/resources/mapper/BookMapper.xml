<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jp.educure.bookmanagement.mapper.BookMapper">

    <select id="findAll" resultType="jp.educure.bookmanagement.entity.Book">
        SELECT id, title, author, price, created_at AS createdAt
        FROM books
        ORDER BY id
    </select>

    <select id="findById" parameterType="int" resultType="jp.educure.bookmanagement.entity.Book">
        SELECT id, title, author, price, created_at AS createdAt
        FROM books
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="jp.educure.bookmanagement.entity.Book">
        INSERT INTO books (title, author, price, created_at)
        VALUES (#{title}, #{author}, #{price}, NOW())
    </insert>

    <update id="update" parameterType="jp.educure.bookmanagement.entity.Book">
        UPDATE books
        SET title = #{title}, author = #{author}, price = #{price}
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM books WHERE id = #{id}
    </delete>

</mapper>
